# Repo Structure

```
Kevyt/
  crates/
    compositor/   Wayland compositor and window manager
    shell/        Panel, dock, launcher, notifications (GTK4)
    settingsd/    Settings daemon and IPC service
    shared/       Shared types and config schema
  docs/
    spec.md
    visual-system.md
    repo-structure.md
  src/            Legacy scaffold (to be removed later)
  Cargo.toml      Rust workspace
```
